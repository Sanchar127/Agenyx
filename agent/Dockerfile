FROM python:3.14-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

# Create a dedicated non-root user.
RUN groupadd --system --gid 10001 agenyx \
    && useradd --system --uid 10001 --gid 10001 \
       --create-home --home-dir /home/agenyx agenyx

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Application files must be readable by the non-root user.
RUN chown -R 10001:10001 /app /home/agenyx

USER 10001:10001

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
