apiVersion: v1
kind: Secret
metadata:
  name: agenyx-agent-secret
  namespace: agenyx
  labels:
    app.kubernetes.io/name: agenyx-agent
    app.kubernetes.io/part-of: agenyx

type: Opaque

stringData:
  # Development-only secret.
  # Replace before production.
  AGENTYX_AGENT_API_KEY: "agenyx-agent-dev-key"
